# 完美钢琴纸带打孔系统

将 MIDI 乐曲转换为「完美钢琴」机械纸带打孔数据，并提供从编曲到上机打孔的一体化工具链。

「完美钢琴」是一台机械纸带钢琴：双排键盘（上排和弦、下排主旋律）各 14 个白键，纸带自下而上走带，孔位经过滚轮触发拨片发声。换挡键孔（◀◀/▶▶）可移动键盘音域窗口（每次 14 个白键），使有限的键位覆盖更宽的音域。

## 仓库结构

```
play/
├── perfect-piano/            纸带打孔工作室（网页应用，零依赖）
│   ├── index.html            主程序入口（双击即用）
│   ├── test.html             自测页（137 项断言）
│   ├── js/
│   │   ├── pp-tape.js        纸带数据模型与 MIDI 转换管线（核心）
│   │   ├── app.js            主程序：渲染 / 编辑 / 播放 / 导出
│   │   ├── midi-parser.js    MIDI 文件解析
│   │   ├── synth.js          Web Audio 发声模拟
│   │   └── punch-sender.js    打孔机上位机（Web Serial）
│   ├── test/                 换挡逻辑测试曲（MIDI）
│   └── tools/                测试曲生成脚本
├── puncher/
│   └── firmware/puncher/     Arduino Nano 打孔机固件（puncher.ino）
├── midi/                     示例曲（梦中的婚礼、土耳其进行曲）
├── 完美钢琴.txt               纸带钢琴原始规格
├── 打孔机.md                 打孔机硬件规格
└── 打孔机-设计方案.md         打孔机设计文档（架构 / 标定 / 协议）
```

## 快速开始

1. **打开工作室**：双击 `perfect-piano/index.html`（推荐 Chrome / Edge，零安装零依赖）。
2. **导入乐曲**：点击「导入 MIDI」选择 `.mid` 文件，或使用「示例曲」直接体验。
   - 多轨文件默认按音域均值自动分配：最高轨 → 主旋律（下排），其余轨 → 和弦（上排）。
   - 单轨文件默认开启「按音高拆分」，低于 C4 的音自动归入和弦排。
3. **预览试听**：播放模拟真实纸带机行为（含换挡时序、双排 105mm 站间错位）。
4. **导出图纸**：导出 1:1 SVG / PNG 用于参考，或保存工程 JSON 后续编辑。

## MIDI 转换管线

- 黑键就近映射到白键（同距取低），自动移调优先减少半音损失
- 超出当前 14 键窗口时自动调度换挡键孔：基于真实时间提前量（400ms 换挡 + 200ms 余量）
- 密集音符物理无法打孔时（同轨孔心距 < 4.5mm）自动整曲拉长每拍毫米数，节奏零失真
- 同拍双排孔在带长方向相距 105mm（两排滚轮的物理站间距）

## 打孔机

网页端「打孔机」面板通过 Web Serial（115200）控制 Arduino Nano 固件，完成自动打孔：

- **固件**：`puncher/firmware/puncher/puncher.ino`（Arduino Nano + 2×THB6128 + PWM 舵机）
  - X 轴：Φ24mm 滚轮走带（339.53 脉冲/mm）；Y 轴：1模21齿齿轮齿条（388.03 脉冲/mm）
  - 梯形加减速脉冲引擎、打孔状态机（下压 45° → 钻孔 2s → 抬起 90°）
  - ASCII 行协议：`V/O/J/P/Z0/Z1/F/C/Q/R`；EEPROM 保存标定当量；急停与预留限位
- **上位机**：工作室内打孔面板——点动对刀、设原点、当量校准、逐孔 ACK 流控、暂停/急停、`.punch` 任务文件导出

使用流程：连接 → 点动对刀 → 设为原点 → 生成任务 → 开始打孔。

详细设计见 [打孔机-设计方案.md](打孔机-设计方案.md)。

## 自测

浏览器打开 `perfect-piano/test.html`，覆盖白键映射、换挡调度、最密间隔拉长、端到端转换、打孔任务生成等 137 项断言。

## 硬件规格摘要

| 项 | 值 |
|---|---|
| 纸带宽 | 150mm（含两侧 13mm 留边） |
| 每排轨数 | 16（◀◀ + 14 白键 + ▶▶），轨距 8mm，上下排错开 4mm |
| 每拍走带 | 9mm（默认，密集曲自动拉长） |
| 两排站间距 | 105mm（沿走带方向） |
| 换挡 | ±1 挡 ×2 个八度，耗时 400ms，提前量 600ms |
| 最小孔距 | 4.5mm（同轨孔心距） |
