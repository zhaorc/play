* 1、纸带钢琴.png是手机上的钢琴app的截图，键盘模式，双排键盘，标准键盘（既有白健也有黑键），每个档位是N个白键，N是可调节参数。
* 2、初始档位是A0档，记住：xx档的意思是最左侧是xx键的档位。
* 3、最低档是A0，最高档是D6档，
* 4、单箭头按钮未使用，两侧双箭头按钮点击后切换档位，按一次切换一个档位，换档时间约400毫秒。
* 5、上排弹奏和旋，下排弹奏主旋律
* 6、每个按键（白健、黑键）、双箭头按钮由一个滚轮带动拨片控制，滚轮由打孔纸带驱动（类似纸带音乐盒）
* 7、共有两排滚轮分别控制两排键盘，控制和旋的称为上排滚轮，控制主旋律的称为下排滚轮
* 8、两排滚轮平行排列、在纸带的运动方向上的间距是105mm，为防止干涉，下排滚轮比上排滚轮向右偏移D毫米，D由计算得出。
* 9、纸带总宽度是150mm。档位宽度（最左键的左侧到最右键的右侧的距离）是133mm。
* 10、换挡控制滚轮不占用档位宽度，档位宽度全部用来排布键盘控制滚轮。
* 11、标准键盘的黑键是嵌在相邻的两个白健之间，黑键的长度约为白健的一半。将键盘布局简化一下，简化后的键盘，黑键和白健的长度相同，每两个白健之间都有一个黑键（对于实际不存在的黑键，比如EF之间没有黑键也要插入一个虚拟黑键用来占位），白健与黑键宽度比值是7:6
* 12、纸带的打孔直径为2.5mm，要求孔间距不能小于2.5mm，最外侧孔距离边界不小于2.5mm。
