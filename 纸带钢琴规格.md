* 纸带钢琴.png是手机上的钢琴app的截图，键盘模式，双排键盘，标准键盘（既有白健也有黑键），每个档位是N个白键，N是可调节参数。
* 初始档位是A0档，记住：xx档的意思是最左侧是xx键的档位。
* 最低档是A0，最高档是D6档，
* 单箭头按钮未使用，两侧双箭头按钮点击后切换档位，按一次切换一个档位，换档时间约400毫秒。
* 上排弹奏和旋，下排弹奏主旋律
* 每个按键（白健、黑键）、双箭头按钮由一个滚轮带动拨片控制，滚轮由打孔纸带驱动（类似纸带音乐盒）
* 共有两排滚轮分别控制两排键盘，控制和旋的称为上排滚轮，控制主旋律的称为下排滚轮
* 两排滚轮平行排列、在纸带的运动方向上的间距是105mm，为防止干涉，下排滚轮比上排滚轮向右偏移D毫米，D由计算得出。
* 纸带总宽度是150mm。档位宽度（以A0档N=14为例就是A0键的最左侧到G2键的最右侧）是133mm。
* 换挡控制滚轮不占用档位宽度，档位宽度全部用来排布键盘控制滚轮。
* 黑键宽度是白健宽度的60%，
* 纸带的打孔直径为2.5mm，要求孔间距不能小于2.5mm，最外侧孔距离边界不小于2.5mm。
* 按照以上规格，计算合理的N值使得档位的键数最多，并计算D值，设计midi文件转换和播放程序。