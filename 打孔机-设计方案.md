# 打孔机程序设计方案

> 依据：`打孔机.md` 硬件规格（2026-09-14）
> 状态：**已确认，固件 v1.0 已完成**（Nano 编译通过：Flash 29% / RAM 17%；确认结论见第六节）

## 一、系统总体架构

```
┌─────────────────────────┐   USB 串口(有线)   ┌──────────────────────┐
│  PC 端（上位机）          │  文本行协议 115200  │  Arduino Nano 固件     │
│  perfect-piano 网页扩展  │ ◄──────────────► │  ATMEGA328P (2KB RAM) │
│  · 孔位→机器坐标换算      │   P / J / O / R   │  · X/Y 步进脉冲发生    │
│  · 手动对刀/校准/试打      │                   │  · Z 伺服打孔循环      │
│  · 任务流式下发+进度回读   │                   │  · 逐孔 ACK 流控       │
└─────────────────────────┘                   └──────────────────────┘
                                                      │
                              X: THB6128→17HS2408s（滚轮走带）
                              Y: THB6128→17HS2408s（齿轮齿条横移）
                              Z: 舵机/伺服 45°下 / 90°抬
```

数据流复用现有成果：`state.holes`（col,row,lane）→ `PP.holeYMM()` 和 `PP.laneXMM()` → 毫米坐标 → 串口逐孔下发，固件负责脉冲与打孔时序。

## 二、机械标定（关键常量）

128 细分下每圈脉冲 = 200 × 128 = **25600 脉冲/圈**

| 轴 | 传动 | 每圈走距 | 理论脉冲/mm |
|---|---|---|---|
| X（走带长度方向） | Φ24mm 滚轮直连摩擦 | π×24 = 75.40mm | **339.53** |
| Y（纸带宽度方向） | 1模21齿齿轮齿条，节圆 Φ21mm | π×21 = 65.97mm | **388.02** |
| Z | 伺服 | 90°抬 / 45°下，下压保持 **2s** | — |

- 理论值仅作初值，固件支持标定命令微调（补偿滚轮打滑/齿轮公差），存入 EEPROM
- 坐标映射：**机器 X = 纸带长度（holeYMM），机器 Y = 纸带宽度（laneXMM，13~137mm）**
- 行程上限：Y 需覆盖 150mm 带宽（≈5820 脉冲）；X 一首歌可达数米，脉冲数用 int32（上限约 6km，安全）
- 孔按 **机器 x（纸带长度）升序为主、y（宽度）升序为辅** 排列打孔顺序——纸带单向进给不可倒带，同 x 位置的孔先打低 y 再打高 y

## 三、固件设计（`puncher/firmware/puncher/puncher.ino`，已实现并编译通过）

**引脚分配**

| 信号 | 引脚 | 说明 |
|---|---|---|
| X: PUL/DIR/ENA | D2/D3/D4 | THB6128 光耦输入，ENA 低有效 |
| Y: PUL/DIR/ENA | D5/D6/D7 | THB6128 光耦输入，ENA 低有效 |
| X 原点限位（预留） | D8 | INPUT_PULLUP，未装时空置；移动中触发即急停 |
| Z 舵机 | D9 | PWM 50Hz，Servo 库（ATmega328P 硬件 PWM 引脚） |
| 硬件急停按钮（预留） | A0 | INPUT_PULLUP，未接空置；轮询触发等同 `R` 命令 |
| Y 原点限位（预留） | A1 | INPUT_PULLUP，未装时空置 |

**实现要点**

- 脉冲用 `digitalWrite` + `micros()` 定时（10mm/s 进给 ≈ 3.4~3.9kHz，Nano 无压力）；非阻塞状态机，每步按加减速斜坡实时算脉冲间隔，保证 `R` 急停随时可响应
- 单孔动作序列：X 进给到位 → Y 横移到位 → Z 舵机转到 45° → 保持 2.0s → Z 回到 90° → 回 `DONE`
- RAM 只保留单行命令缓冲（2KB 内存限制），靠上位机 ACK 流控（收到 DONE 再发下一孔），不做整块缓存；执行中收到除 `R` 外命令回 `ERR BUSY`
- 上电默认 Z=90°（抬起）、使能步进、等待原点设定；EEPROM 存 magic+脉冲当量，首次烧录自动写默认值
- Z 轴确认为标准 PWM 舵机（3 线，50Hz，脉宽 0.5~2.5ms 对应 0~180°），由 D9 硬件 PWM 输出；舵机须独立供电（5V BEC），勿取 Nano USB 电源

**串口协议**（ASCII 行，`\n` 结尾，应答 `OK/DONE/ERR`）

| 命令 | 含义 |
|---|---|
| `V` | 版本握手，回 `PUNCHER v1.0 READY` |
| `O` | 当前位置设为原点（手动对刀后） |
| `J <x> <y>` | 点动到绝对毫米位置（不打孔），到位回 DONE |
| `P <x> <y>` | 在绝对坐标打一个孔（走完+2s循环，回 DONE） |
| `Z0 / Z1` | 手动抬钻 90° / 压钻 45°（调试用） |
| `F <mm/s>` | 设置进给速度（默认 10，范围 1~50） |
| `C <xpmm> <ypmm>` | 校准脉冲当量，写 EEPROM 永久保存 |
| `Q` | 查询当前位置，回 `POS <xmm> <ymm>` |
| `R` | 急停：立即停脉冲、抬钻，回 `OK ESTOP` |

## 四、PC 端设计（扩展现有网页）

新增 `js/punch-sender.js` + 界面"打孔"面板：

1. **任务生成**：从当前 `state.holes` 直接换算毫米坐标（含换挡键孔，lane 0/15 对应 x=13/133mm），按走带方向排序；可导出 `.punch` 文本任务文件（每行 `P x y`）离线备用
2. **连接与控制**：Web Serial API（Chrome/Edge），连接/断开、115200、版本握手
3. **对刀流程**：手动点动 X/Y → 钻头对准纸带头部第一孔位参考点 → `O` 设原点
4. **执行面板**：试打单孔（不装钻头干跑 Z）→ 试打前 N 孔 → 整曲发送；实时进度（第 k/总孔数、当前坐标），暂停/继续/急停
5. **校准面板**：输入实测走距反推脉冲当量，下发 `C`
6. 安全细节：发送前显示"X 总行程 xx 米 / 共 xx 孔 / 预计 xx 分钟"（每孔 2s+进给时间）需确认；Z 抬起状态失能"开始"

## 五、实施步骤

1. ✅ `puncher/firmware/puncher/puncher.ino` 固件 + 协议解析/脉冲引擎/打孔状态机（2026-09-14 完成，arduino-cli 编译通过）
2. `test.html` 增加协议层纯逻辑断言（脉冲当量换算、排序、命令格式）
3. 网页端打孔面板 + Web Serial + 任务生成/导出
4. 无硬件自测：固件协议用 PC 端串口模拟器对跑（干跑模式，禁 Z）
5. 上机调试：对刀→单孔→多孔→整曲

## 六、确认结论（2026-09-14）

1. **轴向对应**：✅ 机器 X（滚轮走带）= 纸带**长度方向**；机器 Y（齿轮齿条）= 纸带**宽度方向**。固件映射：机器 (x, y) = (holeYMM, laneXMM)
2. **原点方案**：✅ 默认"手动对刀 + `O` 软件设原点"；固件同时预留 X(D8)/Y(A1) 限位引脚与 A0 硬件急停，未安装时空置不影响使用，后期装开关即可扩展自动回零
3. **上位机形态**：✅ 集成进现有 perfect-piano 网页，Web Serial API（Chrome/Edge 桌面；Android Chrome + OTG 亦可），实际使用走 localhost 或 https 安全上下文
4. **Z 轴接口**：✅ 确认为 **PWM 舵机**（50Hz，45° 下压 / 90° 抬起，下压保持 2s），接 D9 硬件 PWM，Servo 库驱动，独立 5V 供电
